name: json-schema
version: 0.0.3
authors:
  - sa-0001
description: |
  JSON-Schema validator
license: MIT

crystal: 0.35.1

dependencies:
  json-typeof:
    github: sa-0001/json-typeof-cr
    version: ~> 0.0.3

development_dependencies:
  tap:
    github: sa-0001/tap-cr
    version: ~> 0.0.2
